info:
  name: Listar Planetas
  type: http
  seq: 1

http:
  method: GET
  url: "{{server.url}}/planets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
