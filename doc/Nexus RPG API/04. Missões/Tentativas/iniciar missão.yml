info:
  name: iniciar missão
  type: http
  seq: 3

http:
  method: POST
  url: "{{server.url}}/attempts"
  body:
    type: json
    data: |-
      {
        "missionId": {{mission.id}}
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const attempt = res.getBody();
        bru.setEnvVar("attempt.id", attempt.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
